# Implementation Tasks: TD Web GUI

Bite-sized tasks derived from [TECH_PROPOSAL.md](TECH_PROPOSAL.md), ordered **vertical-slice-first**: build the thinnest end-to-end path, then deepen it. Each task is scoped for a single focused session.

## How to use this doc

- Work tasks in order within a phase unless a task notes it's independent.
- **Hard dependency: Phase 1 → Phase 2 → {Phase 3, 4, 5}.** Within Phases 3/4/5 most tasks are independent and parallelizable.
- Every task lands its own Vitest coverage where testable. The proposal explicitly wants the connection manager, message parsing, throttle, and echo-suppression logic unit-tested.
- The wire contract is exercised against a **mock TD WebSocket server** (Phase 2.6), not a live `.toe`. The reference `td/` project (Phase 6) is the manual end-to-end check.
- `td-core` ships **zero CSS** and `solid-js` is a `peerDependency` — never bundle Solid.

Status legend: `[ ]` todo · `[~]` in progress · `[x]` done

---

## Phase 1 — Project scaffolding (no business logic)

- [x] **1.1 — Init pnpm workspace.** Root `package.json`, `pnpm-workspace.yaml` (globs `packages/*`, `apps/*`), `.gitignore`, shared base `tsconfig.json`. No application code.
  - _Done when:_ `pnpm install` succeeds on the empty workspace.
- [x] **1.2 — Scaffold `packages/td-core` as a Solid library.** Vite lib mode + `vite-plugin-solid` (JSX-preserving, **not** pre-compiled DOM), `solid-js` as a `peerDependency`, `exports` map with type declarations, package `tsconfig`. Export one trivial symbol.
  - _Done when:_ `pnpm --filter td-core build` emits JSX-preserving output + `.d.ts`.
  - _Note:_ the build uses `tsc` with `jsx: "preserve"` rather than Vite lib mode — `vite-plugin-solid` always pre-compiles JSX to DOM calls (which this task forbids) and Vite's rollup bundler can't emit JSX-preserved output, so `tsc` is what actually delivers JSX-preserving `.jsx` + `.d.ts` with `solid-js` external. `vite-plugin-solid` still powers the example app and Vitest.
- [x] **1.3 — Scaffold `apps/example` Solid app.** Vite + Solid, depends on `td-core` via `workspace:*`, renders the trivial `td-core` export.
  - _Done when:_ `pnpm --filter example dev` shows the symbol in-browser.
- [x] **1.4 — Vitest in `td-core`.** Config + one passing trivial test.
  - _Done when:_ `pnpm --filter td-core test` is green.

---

## Phase 2 — Vertical slice: text & numbers over WebSocket

Minimal end-to-end. **No** reconnect, throttle, backpressure, watchdog, or heartbeat yet — those are Phase 3.

- [x] **2.1 — Wire-format types.** TS discriminated-union envelope for the _subset only_: `hello`, `welcome`, `snapshot-request`, `snapshot`, `update`. Include a `parse(raw): Message | null` that try/catches bad JSON and drops unknown `type`s.
- [x] **2.2 — `createTDConnection(url)` core.** Open WS; run handshake (`hello` → await `welcome` → `snapshot-request` → apply `snapshot`); apply inbound `update`; `send()`; reactive `status` signal (e.g. `connecting`/`open`/`synced`). **Lazy signal allocation:** `signal(name)` creates-or-returns one signal per name; routing table maps name→signal; inbound updates for unbound names are dropped (map miss, no allocation). Usable standalone with zero context.
- [x] **2.3 — Factory + context.** `createTDClient<Schema>()` returns a schema-bound bundle (`{ Provider, signal, ... }`). `<Provider url>` owns one connection and shares it via Solid context. `createTDSignal(name)` binds to the nearest provider's connection.
- [x] **2.4 — `TextInput`, `NumberInput`, `RangeInput`, `Value`.** Bound via `createTDSignal`; optimistic local write + send-on-change. `NumberInput`: hold-last-valid while empty/unparseable, snap-back on blur, clamp to `min`/`max` before sending, never send `NaN`. `RangeInput`: slider bound to a number param; value always in-range so no empty/`NaN`/clamp handling. `Value`: read-only, never sends, optional `format` fn, works for scalars and arrays.
- [x] **2.5 — Focus-based echo suppression.** Track an active-editor **count** on the signal (increment on focus/drag-start, decrement on blur/drag-end); suppress inbound TD updates while count `> 0`. Local edit wins while focused.
- [x] **2.6 — Mock TD WS server + integration test.** Stub WebSocket server doing `welcome` → `snapshot` → `update`; test the full `hello`→`welcome`→`snapshot`→`update` flow plus a malformed-message case.
- [x] **2.7 — Example wiring.** `apps/example/src/td.config.ts` exporting `{ id, url }[]` (host/port overridable via `import.meta.env`), plus a page with a text + number control bound through a provider.
  - _Phase done when:_ the example round-trips a string and a number against the mock (or a hand-run TD).

---

## Phase 3 — WebSocket hardening

All tasks are per-connection options with sane defaults, not wire-format changes. Mostly independent of each other.

- [x] **3.1 — Reconnect + exponential backoff.** ~500ms start, double to ~10s ceiling, with jitter. Re-run the full handshake + snapshot resync on each reconnect.
  - _Note:_ per-connection options `reconnect` / `backoff.{min,max}`. Half-jitter (`base/2 + rand·base/2`). Each attempt gets a monotonic `attemptId`; a torn-down socket's late `close`/`error` events guard on it, so a stale event can't drive a second reconnect.
- [x] **3.2 — Handshake watchdog.** Require `welcome` **and** `snapshot` within ~5s of `onopen`; otherwise abandon the attempt → backoff. Cleared the moment `snapshot` applies (`synced` flips true).
  - _Note:_ the watchdog is armed **before** `hello` is sent — a TD (or the in-memory mock) that replies synchronously can complete the whole handshake inside the `hello` send, and arming after would leave a watchdog `onSynced` already ran past, firing a spurious reconnect.
- [x] **3.3 — `ping`/`pong` heartbeat.** App-level `ping` ~every 5s; a missing `pong` within ~10s marks the socket half-open → forces reconnect. (Guards an _established_ session only — the watchdog covers the pre-handshake window.)
  - _Note:_ the pong deadline is armed only on the _first_ unanswered ping, so when the interval is shorter than the pong timeout later pings don't push it out (a truly half-open socket still trips). `heartbeat: false` disables it. Wire types `ping`/`pong` added.
- [x] **3.4 — Outbound throttle.** rAF-aligned (~60fps) coalescing of `update` sends (multiple param changes in a frame → one message). Per-component rate/opt-out prop.
  - _Note:_ `setValue(v, { throttle: true })` on the binding buffers into one per-frame `update`; the optimistic local signal write stays immediate. `<RangeInput>` throttles by default with a `throttle={false}` opt-out; discrete controls (`NumberInput`, `TextInput`) send immediately.
- [x] **3.5 — Backpressure.** Check `ws.bufferedAmount` against a high-water mark before sending; skip `update`s while above it (latest coalesced value supersedes). Flip a `congested` flag on the status signal; sustained high-water → forced reconnect.
  - _Note:_ `congested` is exposed as its own `Accessor<boolean>` on the connection (kept off the `status` enum so `status()` stays a plain lifecycle string). Sustained congestion past `backpressure.timeout` reconnects.
- [x] **3.6 — Disconnected sends + error handling.** Drop (debug-log) `update`/`pulse` written while `connecting`/`closed` — do **not** queue. Route inbound `error` messages to an errors signal / `onError` (console-log by default, non-fatal). Ignore unknown message `type`s; catch + drop malformed JSON without tearing down the socket.
  - _Note:_ `error` wire type added; routed to `onError` + a `lastError` accessor. (`pulse` drop lands with the pulse message in Phase 4.3 — the update path already drops-and-logs while disconnected.)
- [x] **3.7 — Provider teardown.** `onCleanup`: close WS, cancel reconnect/backoff timer **and** ping interval, drop routing table + per-param signals. Each provider tears down only its own instance.
  - _Note:_ `close()` cancels every timer (reconnect, watchdog, ping/pong, congestion, rAF frame), closes the socket, and clears the routing table. Timers go through an injectable `TDScheduler` (default = platform globals) so the timing paths are deterministically testable.

---

## Phase 4 — Remaining controls + parameter modes

Each control accepts a `name` prop, uses `createTDSignal`, passes through HTML props, and is a member of the `createTDClient<Schema>()` bundle. Mostly independent.

- [x] **4.1 — `Range`.** Slider with optional `min`/`max`/`step`; sends continuous values (throttled by default); reflects TD-side changes.
  - _Note:_ already shipped as `<RangeInput>` in Phase 2.4/3.4 (the codebase settled on the `*Input` naming alongside `TextInput`/`NumberInput` rather than the proposal's bare `Range`) — no new work needed, just ticking the box.
- [x] **4.2 — `Toggle`.** Checkbox bound to a TD bool; sends `true`/`false`; bidirectional.
- [x] **4.3 — `pulse` message + `Button mode="pulse"` (default).** Add `{ type: "pulse", name }` to the wire types. Fire-and-forget, web→TD only, holds no state, **throttle-exempt** (sent immediately, but still respects backpressure — dropped + debug-logged into a congested socket). Excluded from snapshot/echo logic.
  - _Note:_ `connection.pulse(name)` is a dedicated send path (not `signal()`/`setValue`) — no routing-table entry, no signal, so there's nothing for `applyParams`/echo-suppression to touch.
- [x] **4.4 — `Button mode="hold"`.** Momentary bool: `true` on press, `false` on release. Pointer capture on pointerdown; `pointerup`/`pointercancel`/`lostpointercapture`/window `blur` all → release. Keyboard Space/Enter (keydown→`true`, keyup→`false`, suppress key-repeat). Pressed-state ARIA. Normal bidirectional bool `update`.
- [x] **4.5 — `Button mode="toggle"`.** Each click flips a TD bool; same wire path as `Toggle`, rendered as a button.
  - _Note:_ 4.3/4.4/4.5 share one `<Button mode="pulse|hold|toggle">` component (`mode` fixed at setup, like `TextInput`'s `commitOn`/`multiline`) rather than three separate components, matching the proposal's framing of one button with three wire behaviors.
- [x] **4.6 — `Select`.** Dropdown bound to a TD Menu par; `options` is web-authored `{ value, label }[]`; wire value is the menu string **key**; reflects TD-side changes.
- [x] **4.7 — `Vector`.** Group of numeric inputs bound to a non-color multi-component `number[]` ParGroup; `length` (or `labels`) prop sets component count; sends whole array (throttled while dragging); reflects TD. This is the generic array wire shape.
- [x] **4.8 — `Color`.** Color-specialized sibling of `Vector` bound to a `[r,g,b]`/`[r,g,b,a]` array (0–1 floats); `alpha` prop toggles RGB vs RGBA; throttled while dragging.
  - _Note:_ renders a native `<input type="color">` (hex, RGB only) plus a separate 0–1 alpha range when `alpha` is set — the color input has no native alpha channel.
- [x] **4.9 — Multiple components per parameter.** Verify the shared-signal model: all binders of a name share one signal, optimistic writes fan out, focus-suppression count handles overlapping editors. Likely already satisfied by 2.5 — add explicit tests (slider + readout, slider + number input).
  - _Note:_ added `components/sharedSignal.test.tsx` — confirms fan-out both directions (slider↔readout, slider↔number input) and that the editor count (not a boolean) keeps suppression active until every overlapping editor releases.
- [x] **4.10 — Read-only / parameter modes.** Web-side `readonly` set authored beside the schema → bound control renders disabled + dev warn. Handle inbound `param_not_writable` error → `markReadonly(ref)` + re-snapshot to revert the optimistic edit. No wire-format change.
  - _Note:_ `<Provider readonly={[...]}>` forwards into `createTDConnection`'s `readonly` option; `TDBinding.readonly` is a reactive per-name accessor every bound control disables on (explicit `disabled` prop still wins). The runtime safety net lives in `handleError`: a `param_not_writable` marks the ref read-only and re-sends `snapshot-request`, so the optimistic edit that TD silently ignored snaps back once the fresh snapshot lands.
- [x] **4.11 — `TextInput` `commitOn`.** `commitOn` (`'input' | 'enter'`) + `onCommit` props; form-submit and Enter-fallback commit paths; blur commits; Escape reverts; no-op on unchanged value; `commitOn="input"` unregressed. Built for `apps/text-selector` (see [TEXT_SELECTOR.md](TEXT_SELECTOR.md)).
- [x] **4.12 — `apps/text-selector` phrase library → SQLite.** Migrated the phrase library
      (`tabs`/`recent`) off `localStorage` onto a `node:sqlite`-backed `/api/library`, mounted as a
      Vite plugin on the dev and preview servers; `activeTabId` stays in `localStorage` as UI state.
      Node engine requirement bumped to `>=24`. See [TEXT_SELECTOR.md §5](TEXT_SELECTOR.md#5-storage)
      and its T10.

---

## Phase 5 — Video (WebRTC)

Signaling is multiplexed over the **same** WebSocket. `iceServers: []` (host candidates only on localhost). Tested against a faked `RTCPeerConnection`; real media is a manual check in Phase 6.

- [x] **5.1 — Extend wire types** for `rtc-offer`, `rtc-answer`, `rtc-ice`, `streams` (both directions).
  - _Note:_ all four are members of **both** `ClientMessage` and `ServerMessage`, since the browser offers on connect/rebuild while TD offers on its own renegotiations. `parse` keeps `candidate: null` (end-of-candidates) distinct from an absent `candidate`, and preserves an explicit `null` `sdpMid`/`sdpMLineIndex` rather than collapsing it to omitted.
- [x] **5.2 — `createTDVideoStream(config)` core.** `RTCPeerConnection({ iceServers: [] })`; signaling over the WS; trickle ICE both ways including `candidate: null` end-of-candidates → `addIceCandidate(null)`; returns a `MediaStream` signal. **Resolve the open offer-role question** (browser vs TD offers) — whichever offers on connect also offers on rebuild.
  - _Note:_ **resolved as browser-offers** (rationale in the open-question section below). Signaling rides the control socket through a new `connection.subscribe(listener)` hook, so the connection stays ignorant of peers. The peer is built on the **first `snapshot`**, not at construction — offering into a socket whose handshake hasn't completed would wedge it in `have-local-offer` with no answer coming — and since every reconnect also ends in a `snapshot`, that same hook is the WS-recovery trigger (5.6). Inbound ICE arriving before a remote description is queued and flushed after `setRemoteDescription`.
- [x] **5.3 — `streams` mapping.** Map each track `mid` → stream `id`; support multiple tracks per peer; re-send the `streams` map on **every** (re)negotiation so `<Video>` rebinds if `mid`s shift.
  - _Note:_ tracks are stored keyed by `mid` (what `ontrack` reports) with the announced list as a separate signal, so replacing that list on a renegotiation rebinds every `<Video>` without disturbing the decoded media. Re-sending the map on each negotiation is TD's half of this (Phase 6.4).
- [x] **5.4 — `<Video stream="...">`.** Renders the selected stream (defaults to the primary when only one exists); underlying `<video>` is `muted autoplay playsinline` with prop passthrough. Several `<Video>` on one stream id share the one decoded `MediaStream`.
  - _Note:_ `muted` is set as a **property**, not the JSX attribute — the `muted` content attribute only seeds `defaultMuted`, which does nothing for an element created after parse, leaving the video unmuted and so blocked from autoplaying. Video is opt-in per provider (`<Provider video>`): without it no `RTCPeerConnection` is created, which matters across up to 8 instances, and `<Video>` throws a pointed error.
- [x] **5.5 — Per-stream status + rebuild-on-fail.** Monitor `connectionState` (fallback `iceConnectionState`); `failed` — and `disconnected` after ~2s grace — trigger a full **rebuild** (new `RTCPeerConnection` + re-signal), not `restartIce()`. Per-stream status signal drives a "reconnecting" overlay.
  - _Note:_ `streamStatus(id)` refines the peer status per stream — a peer that's `connected` but hasn't delivered _this_ id's track yet reads `connecting`. Status stays `reconnecting` across a rebuild rather than dropping back to `connecting`, so an overlay doesn't flicker off while the replacement negotiates.
- [x] **5.6 — Renegotiation + WS-reconnect recovery.** Handle `onnegotiationneeded` throughout the peer's life. Media is **not** torn down on a WS blip — on WS reconnect, check each peer's `connectionState` and rebuild only dead ones. `negotiationPending` flag records negotiation requested while the WS is down; flush it on reconnect.
- [x] **5.7 — Faked `RTCPeerConnection` tests** for signaling + rebuild paths.
  - _Note:_ `testing/mockRTC.ts` (peer + `MediaStream` + track fakes) drives `video.test.ts`. It fires `onnegotiationneeded` from `addTransceiver` like a real browser does, because the connection relies on that event — never on `addTransceiver` returning — to send its offer.
- [x] **5.8 — Video peer cleanup.** Provider teardown closes the `RTCPeerConnection` and calls `stop()` on every received track / `MediaStream` so the hardware decoder is freed.
  - _Note:_ the same teardown runs on every **rebuild**, not only on unmount — otherwise each failure would strand a decoder. A monotonic `peerId` invalidates in-flight async signaling, so a late `createAnswer` can't send SDP for a peer that's already gone.

---

## Phase 6 — TD reference project + integration examples

Reference TouchDesigner project under `td/`, plus the end-to-end example app pieces. Python/TD callbacks kept as version-controlled text beside the `.toe`.

- [x] **6.1 — Registry module.** Python `friendly name → { op, par, type, writable }` map. `type` ∈ `bool`/`number`/`string`/`number[]`.
  - _Note:_ `REGISTRY` in `td/config.py` (and the fuller demo copy in `td/config-example.py`) is a **hand-authored static dict**, not a dynamically built or introspected registry — each entry's `type` is a human-declared literal, not inferred from the backing par's actual TD datatype. `writable` is optional and defaults to `True`. It's the single source of truth 6.2's coercion, mode-guard, and menu logic all read from (`_pars`, `_read`, `_snapshot`, `_write`, `_refuse_write` in `td/webserver-callbacks.py`). `TODO.md` has an open item to go the _other_ direction — auto-creating TD custom pars from `REGISTRY` — so today the registry describes the web schema by hand rather than TD deriving it or vice versa.
- [x] **6.2 — Read/write DAT callbacks.** Coercion both directions per the wire-type table (menus carry the string key; arrays map to a fixed-order ParGroup). Mode-guard on write: skip + emit `param_not_writable` if any backing par isn't `CONSTANT`; per-component check for arrays.
  - _Note:_ **the mode guard protects TD's project, not just the user's edit.** The proposal assumed a write to an expression-driven par would quietly no-op; measured on 2025.33070, `par.val = v` **flips the par to `CONSTANT` and detaches its expression permanently** (the text stays in `par.expr`, unevaluated, and no Python error is raised). An unguarded web write therefore vandalises a TD author's work rather than merely failing. [TECH_PROPOSAL.md](TECH_PROPOSAL.md) § _Parameter modes_ has been corrected to match.
  - _Note:_ `pulse` is deliberately **not** mode-guarded — `par.pulse()` was measured to leave the mode untouched (an `EXPRESSION` par is still `EXPRESSION` afterwards), so it can't detach anything. It honours the registry's `writable` flag only.
  - _Note:_ arrays now resolve through `base.parGroup[name]` instead of a `pars('Color*')` name glob. The glob was wrong twice over: it swept up unrelated pars sharing the prefix (a `Colormode` beside `Colorr/g/b/a`) and ordered by the operator's par list rather than by component. The ParGroup's own order _is_ the wire array order. `broadcast_param_change` matches on the group's real components for the same reason.
  - _Note:_ **an unknown menu key silently selects TD's first menu entry** — no exception — and the Parameter Execute DAT then broadcasts that back as if the user had chosen it. Since `<Select>`'s options are web-authored by design, drift is expected, so writes validate against `par.menuNames` first. This is the failure the "menus carry the string key" line was really guarding.
  - _Note:_ one error code added, `param_type_mismatch` (wrong JSON type, wrong array length, unknown menu key), alongside the `missing_param` this file already emitted. `td-core`'s `parse()` accepts unknown codes and `handleError` acts only on `param_not_writable`, so it's surfaced non-fatally with no web-side change. Coercion is all-or-nothing per param: every component is converted before any is assigned, so a bad third component can't half-apply a colour.
  - _Verified_ against the running `Example.toe`: full snapshot round-trip with correct JSON types; a refused write leaves the par in `EXPRESSION` mode with its expression still driving; a half-constant ParGroup is refused whole with the other components untouched; bad menu key leaves the par alone. Every frame the callbacks emit was fed through `td-core`'s compiled `parse()` and accepted, and `td-core`'s 145 tests still pass.
  - _Note:_ **`menus` — one deliberate wire addition, and the only TD → web introspection in the protocol.** Driven by the audio-device dropdown in `apps/example`: an Audio Device In CHOP's `device` menu has machine-specific keys (`{0.0.1.00000000}.{feb5e51a-…}||Voicemeeter_Out_A4_(VB-Audio…)||1`) with separate readable labels, and the list changes when hardware is plugged in — so it is not authorable on the web at any price, which is the test for admitting a menu to this exception. `<Select>`'s `options` prop is now optional and always wins when present, so no existing select changes. TD announces every menu-backed `string` param (no registry flag — a par either has `menuNames` or it doesn't; `bool` params are excluded because TD Toggles carry `menuNames` too but render as checkboxes). See [TECH_PROPOSAL.md](TECH_PROPOSAL.md) § _TD-announced menus_.
  - _Note:_ **two real `<Select>` bugs fell out of this, both invisible with a static option list.** (1) `<For>` recycles `<option>` elements in place, so a changed list kept the selected _index_ while the values shifted underneath — the dropdown silently showed a neighbouring device as selected. Selection is now bound per-option (`selected={…}`), following the data rather than the position; an effect re-asserting `select.value` does **not** fix it, since it runs before `For` updates the DOM. (2) A value TD reports that has no matching option (device unplugged while selected) made `<select>` display some _other_ option as though the user had chosen it; it now renders as a disabled "(unavailable)" entry, so the UI never misreports TD's state.
  - _Note:_ **this is an open TouchDesigner bug, confirmed still present on 2025.33070**, not a gap in our design. Derivative logged it in April 2021 ([forum](https://forum.derivative.ca/t/breaking-binding-a-dropdown-menu-out-to-a-perform-ui/13123) — staff: "the menuNames and menuLabels members are changing so they should be dependable"). Measured directly here: a Parameter DAT with Menu Names/Labels output watched by a DAT Execute fires `onTableChange` **0 times** on a `menuNames` change and **1 time** on a _value_ change of the same par — so the wiring is sound and the menu change genuinely doesn't notify. **Don't retry that route.** Jarrett's `MenuWatcher.toe` workaround from that thread is no help to us either: its mechanism is a manual Update pulse (what our Reload button already is) and its payload is syncing a _TD dropdown widget_, which we don't have — our dropdown is in the browser, so we only ever need to _read_ `menuNames` at a moment of our choosing, which always works.
  - _Note:_ menu _contents_ changing has **no TD callback** — a Parameter Execute DAT fires on value/mode/enable/export, and plugging in an interface changes none of them (the value is untouched; only the set of legal values grows). Best option when it exists (from that thread): if a TD _action_ rebuilds the menu — a Screen Grab TOP's Refresh Sources — hook that pulse via `onPulse` and re-announce, which is a true event needing neither poll nor button. Audio devices don't qualify: the OS changes that list, not a par. So something must look again, and there are two ways: **`menus-request`** (web → TD), driving the "Reload devices" button beside the dropdown via `connection.requestMenus()` — the default, since whoever plugged the device in is right there; and an optional **TD-side poll**, `broadcast_menus_if_changed()` on a slow Execute DAT, **not wired up by default** because it costs a `menuNames` read per menu par per tick forever. Both funnel through the same diff, so an unchanged list sends nothing. A real change is **broadcast to every client**, not just the requester — a new device is news for every open browser.
  - _Verified_ live in Chrome against the running TD: 13 real devices announced with readable labels; selecting one in the browser set the exact GUID key on TD's par; changing it in TD updated the browser; a simulated unplug re-announced 12 options and left the selected-but-gone device visible, disabled, and still matching TD's value; the Reload devices button took the list 13 → 11 in place, with no page reload. Against the live module, a `menus-request` with nothing changed replies only to the requester while a real change broadcasts to all — one delivery to the requester either way. `td-core` is at 157 tests passing.
- [x] **6.3 — Handshake/snapshot/broadcast callbacks.** `hello`→`welcome` (with `protocol` + optional instance metadata), `snapshot-request`→`snapshot` (all exposed params), `update` broadcast to all clients, `ping`→`pong`.
  - _Note:_ all four flows live in `onWebSocketReceiveText` (`td/webserver-callbacks.py`). `hello` replies with `welcome` carrying `protocol` and the WebGuiServer's `Identifier` par as instance metadata. `snapshot-request` answers `menus` (if any TD-announced menus exist) then `snapshot` with every registry-exposed, non-`pulse` param via `_snapshot()`. `update` writes route through `_write()`, whose success fires the Parameter Execute DAT into `broadcast_param_change()` → `_broadcast()`, which iterates the module-level `clients` set and sends to every open socket, not just the writer. `ping` replies `pong` directly. Unknown/malformed message types are dropped silently rather than tearing down the socket, so a newer web client can add message types without breaking an older TD project.
- [x] **6.4 — WebRTC signaling callback.** WebRTC DAT + Video Stream Out TOP wired; SDP/ICE relayed over the Web Server DAT's WebSocket; `streams` announce.
  - _Note:_ split across two files by direction — `td/webserver-callbacks.py` takes `rtc-offer`/`rtc-answer`/`rtc-ice` inbound and owns the `connection ↔ client` map (signaling must reach the one browser owning a peer, not every client); `td/webrtc-callbacks.py` relays answers, local ICE, and the `streams` map back out, reaching the sockets through `send_signaling()`. The `mid` in `streams` is parsed out of the negotiated SDP rather than authored — the browser picked it when it built the offer, and a renegotiation can shift it.
  - _Note:_ the WebRTC DAT's `onIceCandidate` passes `(candidate, lineIndex, sdpMid)` — the reverse of how the wire message reads, so those are named rather than passed positionally.
  - _Note:_ **one Video Stream Out TOP serves one peer.** The TOP has a single connection par, so a second browser re-points it and takes the stream from the first. Multiple simultaneous viewers need one TOP per client — **decided in 6.7: single-viewer per instance, newest-wins, and the callbacks now say so out loud.**
  - _Note:_ **`addTrack` is what puts the media in the SDP**, and must precede `createAnswer` — the docs say so explicitly. Attaching the Video Stream Out TOP does _not_ substitute for it: the TOP's `webrtc`/`webrtcconnection`/`webrtcvideotrack` pars are menus that only _select among tracks that already exist_, so without `addTrack` the answer negotiates cleanly and comes back `a=inactive`. That failure is near-invisible — `connectionState: connected`, a live receiver, no error on either side, just no pixels. Pointing the TOPs at the track is therefore a separate step, deferred one frame so the DAT has cooked and published those menus; it feeds pixels and never touches the SDP.
  - _Note:_ TD's built-in pars are **lowercase** (`webrtc`, `mode`); only custom pars are capitalized like `REGISTRY`'s. Getting this backwards fails silently.
  - _Verified_ against faked TD globals: `openConnection` → `addTrack` → `setRemoteDescription` → `createAnswer`, with the TOP untouched at answer time and wired a frame later; rebuild closes the old peer before opening a new one; socket close closes the peer and empties both maps. Every frame the Python emits was fed through `td-core`'s compiled `parse()` and accepted. **Confirmed live end-to-end** against a running TD: real media, real peer, browser rendering the stream.
  - _Note:_ TD's WebRTC output arrives **mirrored in X** even when the TD viewer is correct — fix with a Flip TOP (`flipx`) ahead of the Video Stream Out TOP, not a CSS transform. Derivative's own palette component uses CSS and it breaks in Chrome fullscreen. Belongs on 6.8's checklist: only real pixels reveal it.
- [ ] **6.5 — Minimal `.toe`/`.tox`.** Everything wired together; Web Server DAT bound to **`127.0.0.1`** (deliberate loopback bind — the closed-system guarantee).
- [x] **6.6 — Multi-instance example.** 2+ factories/providers with heterogeneous schemas on distinct ports; independent connection lifecycles.
  - _Note:_ two `.toe` files — `apps/example/td/Example1/Example.toe` (port 9980) and `Example2/Example2.toe` (port 9981) — driving a two-column page. `apps/example` holds one `createTDClient<Schema>()` per instance and one `<Provider>` per column; `StatusBar` and `VideoWall` are built from the bare `useTDConnection`/`useTDVideoStream` instead of from either factory, since neither names a parameter, so one component serves both columns.
  - _Note:_ **the schemas are heterogeneous in names, not just in size, and that is the load-bearing part.** Instance 2 registers `label`/`opacity`/`playing`/`restart`/`tint` against the _same_ backing pars instance 1 exposes as `message`/`intensity`/`enabled`/`reset`/`color`, plus a strict subset of the kinds (no menu, no table readout, no 3-component Vector). A wire name belongs to its instance, so the factories make crossing them a compile error rather than a silently dropped `update`. Instance 2 also carries the project's only `writable: False` entry (`opacity`) — the flag never crosses the wire, so it is the one place the TD-side backstop and the web-side `readonly` set agree by convention and can be made to disagree on purpose.
  - _Note:_ the eight-stream target of 6.7 is now **four tiles per instance**, not eight on one — same total encoder load, two peers instead of one. Example2's four are literally Example1's old tiles 5-8, kept and renumbered, so the two walls take opposite halves of one tint palette: a tile rendered under the wrong instance is as visible as a mis-mapped `mid` is within one. Stream ids repeat (`tile1`…`tile4` in both) because an id only ever resolves inside its own provider's stream map.
  - _Note:_ **found and fixed at two instances: `td-core` was starting every inbound signaling message concurrently.** `handleOffer`/`handleAnswer`/`handleIce` were each `void`-called straight from the subscribe handler, so two offers arriving in one tick interleaved — the second `setRemoteDescription` undid the first, and the first's `setLocalDescription` then threw `InvalidStateError: Called in wrong signalingState: stable`, leaving TD wedged in `have-local-offer` with a `connected` peer and no pixels. TD renegotiates more than once while attaching its tracks, so back-to-back offers are ordinary traffic; this was latent at one instance and reproducible at two. Fixed with a signaling operations chain (`enqueueSignaling`), the standard perfect-negotiation serialization, with the peer id captured at arrival so a queued step still refuses a rebuilt peer. `mockRTC` now enforces the real state machine (an answer with no offer to answer throws, as Chrome does) — a permissive fake had been passing the exact sequence that fails live.
  - _Verified_ live against both running projects: both columns synced, 4 + 4 = 8 streams announced and decoding distinct tracks on mids 0-3 each, TD holding 60fps. Write isolation confirmed in both directions on the same backing par (`label` on instance 2 left instance 1's `message` untouched, and vice versa). Independent lifecycles confirmed by disabling instance 2's Web Server DAT: only its column fell to "reconnecting…", and it recovered its socket _and_ its video wall on its own when re-enabled. Clean console, `td-core` at 172 tests passing.
- [x] **6.7 — Video-at-scale grid.** Up to 8 `<Video>` tiles visible at once, ≤720p/≤30fps target; peers reused per instance.
  - _Note:_ TD side is `/project1/videowall` — source → `res_cap` → `level_tile1…8` → `videostreamout_tile1…8`, i.e. **one Video Stream Out TOP per stream, all eight tracks on the instance's one peer**. `res_cap` (Resolution TOP, `limit` 1280×720) is the single place the ≤720p target is enforced; each stream-out is a constant 30fps rather than the default `me.time.rate`, so eight encoders don't each run at the project's 60. Web side is `<Provider video={{ receivers: 8 }}>` — the offer must carry all 8 recvonly m-lines up front, since an answerer can't add m-lines.
  - _Note:_ **the tiles are tinted per-stream on purpose.** Eight copies of one source would have hidden the mid-to-id bug below; a scrambled colour order can't. (The tint is a per-channel multiply — the sample movie is greyscale, which a hue offset cannot touch.)
  - _Note:_ **found and fixed at 8 tracks: every tile rendered stream 1.** TD announces _all_ of a peer's tracks inside a single msid (`TouchDesigner_webrtc1`), so `ontrack`'s `event.streams[0]` is the same N-track object on every mid — and a `<video>` plays only the first video track of whatever it's handed. `registerTrack` now wraps each track in its own `MediaStream` keyed by mid. Invisible at one stream, which is why 5.7's fake was also fixed to deliver one shared stream like TD really does, plus a regression test at 8 tracks.
  - _Note:_ **multi-viewer decision (owed by 6.4): single-viewer per instance, newest-wins.** One TOP holds one connection, so a second browser re-points all eight and the first one's tiles freeze — while its peer stays happily `connected`, with no error to show for it. Serving two browsers needs a second set of eight TOPs. `_handle_rtc_offer` now warns in the textport and sends the newcomer a non-fatal `video_single_viewer` error, so the limit is visible rather than inferred from frozen pixels.
  - _Verified_ live end-to-end: 8/8 streams announced, all 8 tiles decoding distinct tracks in palette order on mids 0–7, 640×360 (Chrome adapting some to 480×270). TD held 60fps at 6.8ms/frame with no new dropped frames; +315MB CPU and no GPU-memory growth for the eight encoders.
  - _Superseded by 6.6:_ the eight tiles are now four per instance across two `.toe` files. Everything above still holds — eight simultaneous tiles at the same encoder cost — but the wall described here (one peer carrying all eight) no longer exists in the reference project. The 8-on-one-peer path is the one 6.6's signaling-serialization bug was hiding in: it was latent here and only became reproducible with two peers negotiating at once.
- [ ] **6.8 — Manual end-to-end checklist** doc covering the live-TD checks that can't be faked (real WebRTC media, reconnect on `.toe` reload).

---

## Open question resolved during Phase 5

**WebRTC offer role — resolved: the browser offers.** `createTDVideoStream` sends the initial SDP offer on connect _and_ on rebuild, expressing its interest as `recvonly` video transceivers (v1 media is TD → web, so the browser contributes no tracks of its own).

Why this side: only the browser knows when it wants a peer — first sync, or a rebuild after a failure — and browser-offers needs no extra "please offer" message to prod TD into starting. That keeps the connect and rebuild paths byte-for-byte identical, which is the symmetry the question was really about.

TD still offers for its **own** renegotiations, and that isn't a contradiction: only an offerer can add m-lines, so a TD instance that starts a new track has to drive that exchange itself. Inbound offers are therefore handled for the whole life of the peer, and a collision with our own in-flight offer is resolved by the browser yielding (`rollback`, then answer). Because the catalog already carries `rtc-offer`/`rtc-answer` in both directions, none of this is a wire change.

The one thing still owed to Phase 6: if the reference WebRTC DAT turns out to insist on offering first, `offerRole: 'td'` flips the initial role in a single option — the answer path is already implemented and tested.
